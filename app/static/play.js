const token=localStorage.getItem('sofiaQuestToken');if(!token)location.href='/';
let state=null,map=null,marker=null,zone=null,timerInt=null,watchId=null,lastPing=0;
const $=s=>document.querySelector(s);function fmt(sec){sec=Math.max(0,Math.floor(sec));const h=String(Math.floor(sec/3600)).padStart(2,'0'),m=String(Math.floor(sec%3600/60)).padStart(2,'0'),s=String(sec%60).padStart(2,'0');return `${h}:${m}:${s}`}
async function api(url,opt={}){const r=await fetch(url,opt);let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.detail||'Грешка');return d}
function gpsError(err){if(err?.code===1)return 'Location достъпът е отказан. Разрешете го от настройките на браузъра.';if(err?.code===3)return 'GPS проверката изтече. Опитайте отново на открито.';return 'Неуспешен GPS достъп.'}
function getPosition(){return new Promise((resolve,reject)=>navigator.geolocation.getCurrentPosition(resolve,reject,{enableHighAccuracy:true,timeout:15000,maximumAge:0}))}
function ensureLiveTracking(){if(watchId!==null||!navigator.geolocation)return;watchId=navigator.geolocation.watchPosition(async p=>{if(!state||state.game.status!=='running')return;const now=Date.now();if(now-lastPing<10000)return;lastPing=now;try{await api('/api/location/ping',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,lat:p.coords.latitude,lon:p.coords.longitude,accuracy_m:p.coords.accuracy})})}catch{}},()=>{}, {enableHighAccuracy:true,maximumAge:5000,timeout:15000})}
function render(){const g=state.game;$('#teamTitle').textContent=state.team.name;$('#competitionInfo').textContent=state.team.competition_code?`${state.team.competition_name} · ${state.team.competition_code}`:'';if(state.team.logo_url){$('#teamLogo').src=state.team.logo_url;$('#teamLogo').classList.remove('hidden')}$('#progressText').textContent=`Мисия ${Math.min(g.current_index+1,10)}/10`;$('#progressBar').style.width=`${g.current_index*10}%`;$('#penalty').textContent=`наказание +${fmt(g.penalty_seconds).slice(3)}`;clearInterval(timerInt);let base=g.elapsed_seconds,stamp=Date.now();$('#timer').textContent=fmt(base);if(g.status==='running')timerInt=setInterval(()=>$('#timer').textContent=fmt(base+Math.floor((Date.now()-stamp)/1000)),1000);$('#readyCard').classList.toggle('hidden',g.status!=='ready');$('#missionCard').classList.toggle('hidden',g.status!=='running');$('#finishCard').classList.toggle('hidden',g.status!=='finished');if(g.status==='finished'){$('#progressBar').style.width='100%';$('#progressText').textContent='10/10 завършени';$('#finishText').textContent=`Крайно време: ${fmt(g.elapsed_seconds)} (включително наказанията).`;return}if(g.status==='ready'&&state.team.competition_code){const waiting=state.team.competition_status!=='running';$('#readyTitle').textContent=waiting?'Изчакване на старта':'Състезанието стартира';$('#readyText').textContent=waiting?'Организаторът ще стартира всички отбори едновременно. Останете на тази страница.':'Натиснете „Влез в играта“, за да заредите първата задача.';$('#startBtn').textContent=waiting?'Изчакване…':'Влез в играта';$('#startBtn').disabled=waiting}if(g.status==='running'){ensureLiveTracking();const m=state.mission;$('#difficulty').textContent=`Трудност ${m.difficulty}/10`;$('#place').textContent=m.place;$('#missionTitle').textContent=m.title;$('#prompt').textContent=m.prompt;$('#hintText').textContent='';$('#hintBtn').disabled=state.hint_used;$('#answer').disabled=!state.location_verified;$('#answerBtn').disabled=!state.location_verified;$('#locStatus').textContent=state.location_verified?'✅ Локацията е потвърдена за кратко време.':'GPS проверката е задължителна. Нужно е да сте в радиус 5 м.';setupMap(m)}}
function setupMap(m){if(!window.L)return;if(!map){map=L.map('map',{zoomControl:true}).setView([m.lat,m.lon],18);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map)}else map.setView([m.lat,m.lon],18);if(marker)marker.remove();if(zone)zone.remove();marker=L.marker([m.lat,m.lon]).addTo(map).bindPopup(m.place);zone=L.circle([m.lat,m.lon],{radius:5}).addTo(map)}
async function load(){try{state=await api('/api/state?token='+encodeURIComponent(token));render()}catch(e){$('#gameMsg').textContent=e.message}}
$('#startBtn').onclick=async()=>{try{await api('/api/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});await load()}catch(e){$('#gameMsg').textContent=e.message}};
$('#locateBtn').onclick=async()=>{const b=$('#locateBtn');b.disabled=true;$('#locStatus').textContent='Проверявам GPS…';try{const p=await getPosition();const d=await api('/api/location',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,lat:p.coords.latitude,lon:p.coords.longitude,accuracy_m:p.coords.accuracy})});if(d.reason==='accuracy')$('#locStatus').textContent=`⚠️ GPS точността е ${d.accuracy_m} м. Нужно е ≤ ${d.max_accuracy_m} м.`;else if(d.reason==='speed')$('#locStatus').textContent='⚠️ Засечено е неестествено GPS преместване. Опитайте отново след няколко секунди.';else $('#locStatus').textContent=d.ok?`✅ Потвърдено: ${d.distance_m} м от точката (точност ±${d.accuracy_m} м).`:`❌ На около ${d.distance_m} м сте. Нужно е ≤ 5 м.`;if(d.ok)await load()}catch(e){$('#locStatus').textContent=e.message||gpsError(e)}finally{b.disabled=false}};
$('#hintBtn').onclick=async()=>{if(!confirm('Подсказката добавя +5:00 минути наказание. Да я използвам ли?'))return;try{const d=await api('/api/hint',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});await load();$('#hintText').textContent=d.hint}catch(e){$('#gameMsg').textContent=e.message}};
$('#answerBtn').onclick=async()=>{const val=$('#answer').value.trim();if(!val)return;const b=$('#answerBtn');b.disabled=true;$('#gameMsg').textContent='Проверявам GPS и отговора…';try{const p=await getPosition();const d=await api('/api/answer',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,answer:val,lat:p.coords.latitude,lon:p.coords.longitude,accuracy_m:p.coords.accuracy})});if(d.ok){$('#answer').value='';$('#gameMsg').textContent='✅ Правилен отговор!';await load()}else $('#gameMsg').textContent='❌ Не е това. Огледайте мястото отново.'}catch(e){$('#gameMsg').textContent=e.message||gpsError(e)}finally{b.disabled=false}};
load();setInterval(async()=>{if(state?.game?.status==='running'||state?.game?.status==='ready'&&state?.team?.competition_code)await load()},10000);