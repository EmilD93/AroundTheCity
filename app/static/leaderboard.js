function fmt(sec){const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;return `${h?String(h).padStart(2,'0')+':':''}${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}
async function load(){const code=document.querySelector('#code').value.trim();const r=await fetch('/api/leaderboard'+(code?'?competition_code='+encodeURIComponent(code):''));const rows=await r.json();const b=document.querySelector('#board');b.innerHTML=rows.length?'':'<p class="muted">Няма резултати.</p>';rows.forEach((x,i)=>{const el=document.createElement('div');el.className='board-row';el.innerHTML=`<div class="rank">#${i+1}</div><div class="board-team">${x.logo_url?`<img class="team-logo tiny" src="${x.logo_url}" alt="">`:''}<div><b>${x.team_name}</b><div class="muted">${x.status==='finished'?'Завършил':`${x.completed}/10 мисии`} ${x.competition_code?'• '+x.competition_code:''}</div></div></div><div class="hide-mobile">+${fmt(x.penalty_seconds)}</div><div class="hide-mobile"><b>${fmt(x.elapsed_seconds)}</b></div>`;b.appendChild(el)})}
document.querySelector('#load').onclick=load;const saved=localStorage.getItem('sofiaQuestCompetition');if(saved)document.querySelector('#code').value=saved;load();setInterval(load,15000);